
" use the elflord colorscheme
colorscheme elflord

" show existing tabs with 4 space width
set tabstop=4

" when indenting with '>', use 4 spaces 
set shiftwidth=4

" replaces tab with 4 spaces
set expandtab

" show line numbers
set number

" color text based on syntax
syntax enable
